# Colorize!
alias ls='ls -G'
alias grep='grep --color=auto'

# Such aliases
alias ll='ls -alFh'
alias vimn='vim +NERDTree'

# Much git
alias gss='git status'
alias gdf='git diff'
alias gaa='git add -A'
alias gcm='git commit'
alias gpcb='git push origin HEAD'
alias gsub='git submodule'
alias gsadd='gsub add'
alias gspull='gsub foreach git pull'
alias gh='open https://github.com/aseom?tab=activity'
