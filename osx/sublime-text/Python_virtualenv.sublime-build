{
    "selector": "source.python",
    "cmd": ["$project_path/.venv/bin/activate",
            "&&", "echo", "[virtualenv]",
            "&&", "python", "-u", "$file"],

    // Regular expression to capture error output of cmd.
    "file_regex": "^[ ]*File \"(...*?)\", line ([0-9]*)"
}